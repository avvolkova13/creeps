# Заполненный кабинет для показа — 2026-09-14

Основание: пользователь требует визуально показать менеджеру все пункты ТЗ личного кабинета. Утверждённые палитра, шрифты и элементы управления сохраняются.

- [x] В отдельном компоненте статической версии показать пример профиля: Steam ID, выход, редактируемый trade-URL с локальным сохранением, баланс и переход к пополнению.
- [x] Показать согласованные по суммам примеры пополнения/списаний и два заказа: «Выдано» и «В ожидании», с составом, изображениями и ценами Creeps/рубли. Использовать существующие товары.
- [x] Встроить существующую корзину в кабинет; при первом посещении добавить два товара для показа, сохраняя уже выбранные пользователем товары. После удаления не добавлять их повторно.
- [x] Дать компактную подпись «Пример заполнения»: локальные данные не подтверждают авторизацию, платежи и выдачу. Не менять серверную сессию и реальные API. Выход закрывает пример; повторное открытие доступно кнопкой.
- [x] Проверить расчёты примеров, lint, тесты, обе сборки, ссылки Pages. В браузере проверить 320/390/768/1440 px, заполненность всех блоков, сохранение/ошибку URL, выход/возврат, корзину и отсутствие overflow.

Файлы: `src/components/account-presentation.tsx`, `src/lib/account-presentation.ts`, подключение в `src/app/account/page.tsx`, стили в `src/app/globals.css`, небольшое расширение `src/lib/preview-session.ts` для однократного заполнения корзины. Проверки данных — `tests/account-presentation.test.mjs`; браузерные артефакты — `.qa/account-presentation/`.

Граница: только визуальный кабинет и локальные действия для презентации. Реальные интеграции остаются отложенными. Git-ветку и remotes не менять, чужие изменения не включать.
